import { css } from 'emotion';

export const header = css({
   background: '#2d652f'
});

export const header__logo = css({
   color: 'white',
   display: 'inline-block',
   fontSize: '2rem',
   fontWeight: 700,
   margin: 0,
   textDecoration: 'none',
   padding: '.6rem 0'
});